# Próxima sesión

**Última sesión:** 2026-05-23 — `mission.md` acordado; primera historia simplificada (slice vertical + `GET /api/experiences`).

## Dónde nos quedamos

- **Constitución:** `mission.md` (acordado), `techstack.md`, `roadmap.md`, `api-contract.md`, `changelog.md`.
- **Primera historia:** `primera-historia-especificacion.md` — slice Fase 1 (GET + POST) / Fase 1b (UI).
- **PASO 3** cerrado en esta sesión; **PASO 4** pendiente.

## Siguiente paso (AGENTS.md)

**PASO 4** — Tests **sin** código de producción (roadmap 1.2–1.4):

- Catálogo 2 experiencias + `GET /api/experiences`
- Validación + cuerpo del email
- `POST /api/requests` (ver `api-contract.md`; mocks según `techstack.md`)

**PASO 5** — Revisión de tests por el anfitrión.

**PASO 6** — Implementación Spring Boot hasta verde.

## Frase para retomar en Cursor

> Lee `specs/next-steps.md`, `specs/api-contract.md`, `specs/techstack.md` y `agents.md`. PASO 4: tests 1.2–1.4 sin código de producción.

## Pendiente menor (no bloquea)

- Pulir copy inglés de descripciones (sobre todo antes de 1b).
- Proveedor email en paso 1.5 (modo `log` basta al empezar).
