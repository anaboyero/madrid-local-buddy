# Próxima sesión

**Última sesión:** 2026-06-24 (cerrada — slice 2.1 mergeada a `main`).

## Acuerdos y estado

- **Slice 2.1:** **en `main`** (PR [#4](https://github.com/anaboyero/madrid-local-buddy/pull/4) mergeada). Persistencia H2, `GET /api/requests`, `id` en `201`, 40 tests, smoke M-01/M-02 OK.
- **Fase 1 API + 2.1:** operativas en `main`.
- **Fase 1b (UI):** **aplazada** — seguir API-first cuando retomes.
- **Slice 1.7 (CI JAR smoke):** descartada por ahora.
- **`Dockerfile`** en working tree local: **fuera de alcance** hasta acuerdo explícito.
- **Local:** conviene `git checkout main && git pull` para alinear con remoto.

## Próximo trabajo (sin orden cerrado — elegir en PASO 1)

1. Otra capacidad **API** (Fase 2+): más experiencias, calendario, etc.
2. **Fase 1b** — UI cuando quieras retomar landing/formulario.
3. Opcional: smoke JAR en CI (ex 1.7).

## Siguiente PASO

**PASO 1** — debate y elegir **siguiente slice** (`requirements.md`). Nueva conversación recomendada.

## Frase para retomar

> `main` con 2.1. Elegir siguiente slice (API o UI 1b). Dockerfile aplazado.
